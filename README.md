# SENDER · ON AIR — web final (dirección completa del departamento)

Norte visual: la maqueta del usuario (10-sep-2026): negro cine, anillos de radar con sweep,
HUD mono (coords + TX · ON AIR), título caps gigante con palabra en azul oficial,
transporte de video visible (play/TC/scrub). Decisiones del departamento:
- Tipografía display CAMBIADA a Anton (caps condensada) + Manrope body + Plex Mono HUD (permitido por el usuario).
- Paleta oficial intacta (#ffffff #1e73be #494949 #0085b2) · descripciones de producto reales (i18n 198 claves).
- Hero: film cap-torre.mp4 con TRANSPORTE (scroll scrub lerp + botón play + flechas teclado) + timecode vivo.
- Catálogo: cover con cine.mp4 scrub + 4 productos sticky + stacks 4 tomas + 15 familias.
- Espectro: dial VFO (drag/teclado/scroll) + lectura log + paneles por banda.
- Proyectos: track pinned (desktop) / swipe snap (móvil) con video Rapa Nui.
- Estático sin bundler: gsap 3.12.5 + ScrollTrigger + lenis 1.1.14 CDN. CI: html-validate + LHCI.
